{
  "name": "magicpulse-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run check && npm run check:release && node scripts/build-site.mjs",
    "check": "node scripts/check-site.mjs && node scripts/check-worker.mjs",
    "check:release": "node scripts/check-release-metadata.mjs",
    "check:release:remote": "node scripts/check-release-metadata.mjs --remote"
  }
}
